Cluster_username: "admin" #<- Ontap cluster admin - SET ONLY IF vars_prompt NOT USED
Cluster_password: "password" #<- Ontap cluster password - SET ONLY IF vars_prompt NOT USED

AD_username: "Administrator"
AD_password: "password"
